[project]
name = "get-connected-client"
dynamic = ["version"]
description = "A typed Python client (and optional CLI) for Galaxy Digital's Get Connected API"
requires-python = ">=3.10,<4.0"
authors = [
  {name = "Brian Kohan", email = "bckohan@gmail.com"},
]
license = "MIT"
license-files = [ "LICENSE" ]
readme = "README.md"
keywords = []
dependencies = [
    "httpx>=0.27",
    "pydantic>=2.7",
]
classifiers = [
    "Operating System :: OS Independent",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",

    "Natural Language :: English",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Programming Language :: Python :: 3.15",
    "Topic :: Software Development :: Libraries",
    "Typing :: Typed"
]

[project.optional-dependencies]
cli = [
    "typer>=0.12",
    "rich>=13.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = true

[tool.hatch.build.targets.wheel]
packages = ["src/get_connected_client"]

[tool.hatch.version]
source = "code"
path = "src/get_connected_client/_version.py"
expression = "get_version()"

[project.urls]
"Homepage"      = "https://github.com/SELAHNHCORG/get-connected-client"
"Documentation" = "https://get-connected-client.readthedocs.io"
"Repository"    = "https://github.com/SELAHNHCORG/get-connected-client"
"Issues"        = "https://github.com/SELAHNHCORG/get-connected-client/issues"
"Changelog"     = "https://github.com/SELAHNHCORG/get-connected-client/releases"
"Code_of_Conduct" = "https://raw.githubusercontent.com/SELAHNHCORG/.github/refs/heads/main/CODE_OF_CONDUCT.md"


[project.scripts]
galaxy = "get_connected_client.cli:app"

[dependency-groups]
typing = [
    "pyright>=1.1.390",
    "mypy>=1.19.1",
]
lint = [
    "ruff>=0.15.0",
    "readme-renderer[md]>=45.0",
    "doc8>=1.1.2",
    "bandit[toml,sarif]>=1.9.4"
]
coverage  = ["coverage[toml]>=7.15.4"]
test = [
    { include-group = "coverage" },
    "pytest>=8.3.4",
    "pytest-cov>=6.0.0",
    "respx>=0.21",
    "pyyaml>=6.0",
]
docs = [
    "sphinx>=7.2",
    "furo>=2024.1.29",
    "sphinx-autobuild>=2024.2.4",
    "doc8>=1.1.2",
    "sphinxcontrib-typer>=0.9.1",
]
dev = [
    { include-group = "typing" },
    { include-group = "test" },
    { include-group = "lint" },
    { include-group = "docs" },
    "packaging>=24.2",
    "ipdb>=0.13.13",
    "ipython>=8.18.1",
]

[tool.mypy]
check_untyped_defs = true
ignore_missing_imports = true
mypy_path = ["src"]
packages = ["get_connected_client"]

[tool.pyright]
pythonVersion = "3.10"
typeCheckingMode = "basic"
include = ["src/get_connected_client"]
reportMissingImports = true
reportMissingTypeStubs = false
reportUnusedImport = true
reportDuplicateImport = true

[tool.doc8]
max-line-length = 100
sphinx = true

[tool.pytest.ini_options]
python_files = "test*.py"
norecursedirs = "*.egg .eggs dist build doc docs .tox .git .worktrees .venv __pycache__"
addopts = [
    "--strict-markers",
    "--cov=get_connected_client",
    "--cov-branch",
    "--cov-report=term-missing:skip-covered",
    "-m",
    "not live and not live_write",
]
markers = [
    "live: hits the real production API (read-only); requires GALAXY_API_KEY",
    "live_write: WRITES to the production API; requires GALAXY_LIVE_WRITE_ACK",
]

[tool.coverage.run]
branch = true
source = ["get_connected_client"]
concurrency = ["thread", "multiprocessing"]
parallel = true
relative_files = true
command_line = "-m pytest --cov=get_connected_client"

[tool.coverage.paths]
source = ["get_connected_client"]

[tool.ruff]
line-length = 88
exclude = ["doc", "docs", "dist"]
target-version = "py310"

[tool.ruff.lint]
exclude = ["tests/**/*"]

[tool.ruff.lint.flake8-bugbear]
# typer declares CLI options as argument defaults -- that is the framework's
# idiom, not a mutable-default bug. B008 happens to let most of ours through
# because their annotations (str | None, bool) are recognized as immutable,
# which makes the rule fire only on the ones annotated with our own enums.
extend-immutable-calls = ["typer.Option", "typer.Argument"]

[tool.bandit]
targets = ["src", ".github/scripts"]
exclude_dirs = [
    "./tests",
    "./.venv",
    "./doc",
]

skips = [
    "B101",  # assert_used - assertions are fine in tests
    "B110",  # Try, Except, Pass detected - these are intentional
]
